{
  "identifier": "default",
  "description": "Default permissions for the band heart rate monitor",
  "windows": ["main"],
  "permissions": [
    "core:default",
    {
      "identifier": "core:event:default",
      "allow": [{ "event": "hr-update" }]
    }
  ]
}
