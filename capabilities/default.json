{
  "identifier": "default",
  "description": "Default permissions for the band heart rate monitor",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:event:allow-listen"
  ]
}
